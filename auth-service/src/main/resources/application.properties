# ===================================================================
# DEPRECATED - KHÔNG SỬ DỤNG FILE NÀY NỮA
# ===================================================================
# File này đã được chuyển sang application.yml để:
# 1. Cấu trúc rõ ràng và dễ đọc hơn
# 2. Tránh xung đột cấu hình giữa properties và yml
# 3. Tập trung cấu hình vào một file duy nhất
# 4. Dễ dàng quản lý và maintain hơn
#
# Xem file application.yml để biết cấu hình mới nhất
# ===================================================================

# spring.application.name=auth-service
#
# # JPA Configuration
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#
# # Flyway Configuration
# spring.flyway.enabled=true
# spring.flyway.baseline-on-migrate=true
# spring.flyway.locations=classpath:db/migration
#
# # JWT Configuration
# jwt.secret=phai-la-jwt-secret-key-va-co-do-dai-256-bit
# jwt.expiration=86400000
# jwt.issuer=auth-service
# jwt.audience=studydocs-client
#
# # OAuth2 Configuration
# spring.security.oauth2.client.registration.google.client-id=your_client_id
# spring.security.oauth2.client.registration.google.client-secret=your_client_secret
# spring.security.oauth2.client.registration.google.scope=email,profile