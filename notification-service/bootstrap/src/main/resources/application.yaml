server:
  port: 8087
spring:
  config:
    name: notification-service
  rabbitmq:
    host: ${RABBIT_HOST}
    port: ${RABBIT_PORT}
    username: ${RABBIT_USERNAME}
    password: ${RABBIT_PASSWORD}
    virtual-host: ${RABBIT_VIRTUAL_HOST}
    ssl:
      enabled: true
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST}:${MONGO_PORT}/${MONGO_DB}
      uuid-representation: standard
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: http://localhost:8081
app:
  remote:
    user-service:
      url: ${USER_SERVICE_URL:http://localhost:8087/api/v1/users}
    file-service:
      url: ${USER_SERVICE_URL:http://localhost:8084/api/v1/files}
  notification:
    email:
      from: ${EMAIL_HOST}
  error:
    code: ${ERROR_CODE}
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG

