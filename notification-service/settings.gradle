rootProject.name = 'notification-service'
include(
        "api",
        "application",
        "infrastructure",
        "domain"
)

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
