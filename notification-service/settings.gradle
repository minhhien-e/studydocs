rootProject.name = 'notification-service'
include(
        "api",
        "application",
        "infrastructure",
        "domain",
        "bootstrap"
)

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
